create table news(
    id int auto_increment primary key,
    days date not null,
    news text not null,
    created_at timestamp default CURRENT_TIMESTAMP
);

INSERT INTO news(days, news) VALUES(:days, :news)